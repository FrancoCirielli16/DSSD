[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: necesita Bonita Studio corriendo y el proceso desplegado (RUN_BONITA_INTEGRATION=1)
filterwarnings =
    ignore::DeprecationWarning
